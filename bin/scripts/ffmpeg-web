#!/bin/zsh

[ $# -eq 0 ] && echo 'Usage: <src> <cq> ...' && exit 1

src="$1"
cq="$2"

shift 2

ffmpeg -i $src -c:v hevc_nvenc -pix_fmt yuv420p -color_range pc -b:v 0 -rc vbr -cq $cq -c:a aac -b:a 0 "$@"
