#!/usr/bin/env ruby
if ARGV.empty? || ARGV.include?("-h") || ARGV.include?("--help")
  puts "Usage: bak <file>  # rename <file> to <file>.bak"
  exit
end
src = ARGV.first
File.rename(src, "#{src}.bak")
