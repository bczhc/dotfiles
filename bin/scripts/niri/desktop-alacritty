#!/bin/zsh

margins=(
    --margin-top 20 --margin-bottom 700 \
    --margin-left 540 --margin-right 540
)
wl-paper \
    --namespace alacritty-bottom \
    --exclusive-zone 0 \
    --layer bottom \
    --keyboard-interactivity on-demand \
    $margins \
    alacritty --config-file /home/bczhc/.config/alacritty/alacritty-bottom-layer.toml --command zsh -c 'tty-clock -sc -C6; exec zsh -i'
