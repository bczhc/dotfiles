#!/bin/bash

[ $# -eq 0 ] && echo 'Usage: cmd <path> [--name]' && exit 1

path="$1"
only_name=0
[ "$2" = '--name' ] && only_name=1

abspath="`readlink -f "$path"`"

if [ $only_name -eq 0 ]; then
    copy="$abspath"
else
    copy="$(basename "$abspath")"
fi

echo "$copy"
echo -n "$copy" | xc
