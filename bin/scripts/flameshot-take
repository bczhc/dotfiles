#!/bin/bash

my-screenshot no-notify
focusedwindow=$(xdotool getactivewindow)
QT_ENABLE_HIGHDPI_SCALING=0 flameshot gui  >/dev/null
if [ "$focusedwindow" == "$(xdotool getactivewindow)" ]
then
	xdotool windowfocus $focusedwindow
fi

