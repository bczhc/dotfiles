#!/usr/bin/env ruby
if ARGV.empty? || ARGV.include?("-h") || ARGV.include?("--help")
  puts "Usage: unbak <file>  # rename <file>.bak back to <file>"
  exit
end
src = ARGV.first
dst = src.end_with?(".bak") ? src[0..-5] : src
bak = src.end_with?(".bak") ? src : "#{src}.bak"
File.rename(bak, dst)
