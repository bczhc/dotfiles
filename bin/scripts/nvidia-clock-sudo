#!/bin/bash

[ $# -ne 3 ] && echo 'Usage: cmd g <min>,<max> | cmd m <min>,<max>' && exit 1

min="$2"
max="$3"

if [ "$1" == "m" ]; then
    nvidia-smi -lmc "$min","$max"
elif [ "$1" == "g" ]; then
    nvidia-smi -lgc "$min","$max"
else
    echo 'Unknown usage'
    exit 1
fi
