[Unit]
Description=ActivityWatch and AWatcher Stack
After=graphical-session.target

[Service]
Type=exec
ExecStart=/bin/zsh -c 'echo $NIRI_SOCKET'
Restart=on-failure
#Environment=WAYLAND_DISPLAY=wayland-1

[Install]
WantedBy=graphical-session.target
